import time
import torch.nn as nn
import torch

from tqdm import tqdm

# pbar = tqdm(total=20)
# for i in range(20):
#     pbar.update(1)
#     time.sleep(0.1)
# pbar.close()


# lstm = nn.LSTM(100, 200 // 2, num_layers=1, bidirectional=True)
# hidden = (torch.randn(2, 1, 200 // 2), torch.randn(2, 1, 200 // 2))
# embeds = torch.randn(60, 1, 100)
# lstm_out, hidden = lstm(embeds, hidden)
# print(lstm_out.size())
# print(hidden[0].size())
# print(hidden[1].size())


# a = 1
# b = 2
# c = a + \
#             b
# print(c)
